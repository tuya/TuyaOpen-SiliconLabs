# Only what this platform needs *beyond* TuyaOpen's own pyproject.toml, which
# already pins cmake, ninja, GitPython, PyYAML and requests for every platform.
# Repeating them here re-resolves the shared .venv and silently downgrades it --
# `cmake~=3.26.3` used to pull the tree back from the 4.0.2 the SDK asks for,
# for every platform built afterwards in the same workspace.
#
# Jinja2 is slc's template engine. platform_prepare.py::_ensure_jinja2 also
# installs it into slc's own bundled interpreter, which is not this .venv.
Jinja2~=3.1.2
